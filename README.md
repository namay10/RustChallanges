# rust

Small Rust practice repo: mini-projects + exercises from the Rust Book, YouTube bootcamps, and RiseIn challenges.

## Folders
- `helo_cargo/` – first Cargo project(s)
- `cli/` – CLI experiments
- `state/` – state/struct/ownership practice
- `web_server/` – basic web server practice
- `risein/` + `riseinFinal/` – RiseIn challenges (git submodules)
